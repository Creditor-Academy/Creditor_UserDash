name: CI/CD Pipeline

on:
  push:
    branches: [ "**" ]
    paths:
      - 'Creditor_UserDash/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [ "**" ]
    paths:
      - 'Creditor_UserDash/**'
      - '.github/workflows/ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        
    - name: Cache npm dependencies
      uses: actions/cache@v4
      with:
        path: Creditor_UserDash/node_modules
        key: ${{ runner.os }}-node-${{ matrix.node-version }}-${{ hashFiles('Creditor_UserDash/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-${{ matrix.node-version }}-
        
    - name: Install dependencies
      working-directory: ./Creditor_UserDash
      run: npm ci
      
    - name: Run linter
      working-directory: ./Creditor_UserDash
      run: npm run lint
      
    - name: Run tests
      working-directory: ./Creditor_UserDash
      run: npm run test:run
      
    - name: Run tests with coverage
      working-directory: ./Creditor_UserDash
      run: npm run test:coverage
      
    - name: Run E2E tests (optional - can be skipped in CI)
      working-directory: ./Creditor_UserDash
      continue-on-error: true
      run: npm run test:e2e || echo "E2E tests skipped"
      
    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      with:
        file: ./Creditor_UserDash/coverage/lcov.info
        flags: frontend
        name: frontend-coverage
        
  build:
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        
    - name: Cache npm dependencies
      uses: actions/cache@v4
      with:
        path: Creditor_UserDash/node_modules
        key: ${{ runner.os }}-node-20.x-${{ hashFiles('Creditor_UserDash/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-20.x-
        
    - name: Install dependencies
      working-directory: ./Creditor_UserDash
      run: npm ci
      
    - name: Build application
      working-directory: ./Creditor_UserDash
      run: npm run build
      
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-files
        path: Creditor_UserDash/dist/
        
  deploy-staging:
    runs-on: ubuntu-latest
    needs: [test, build]
    if: github.ref == 'refs/heads/develop'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        
    - name: Install dependencies
      working-directory: ./Creditor_UserDash
      run: npm ci
      
    - name: Build for staging
      working-directory: ./Creditor_UserDash
      run: npm run build:dev
      
    - name: Check deployment secrets
      id: check-secrets
      env:
        NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
      run: |
        if [ -n "$NETLIFY_AUTH_TOKEN" ] && [ -n "$NETLIFY_SITE_ID" ]; then
          echo "secrets-available=true" >> $GITHUB_OUTPUT
        else
          echo "secrets-available=false" >> $GITHUB_OUTPUT
        fi
      
    - name: Deploy to Netlify (Staging)
      if: steps.check-secrets.outputs.secrets-available == 'true'
      uses: nwtgck/actions-netlify@v2.0
      with:
        publish-dir: './Creditor_UserDash/dist'
        production-branch: main
        github-token: ${{ secrets.GITHUB_TOKEN }}
        deploy-message: "Deploy from GitHub Actions - Staging"
        enable-pull-request-comment: true
        enable-commit-comment: true
        overwrites-pull-request-comment: true
      env:
        NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        
  deploy-production:
    runs-on: ubuntu-latest
    needs: [test, build]
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        
    - name: Install dependencies
      working-directory: ./Creditor_UserDash
      run: npm ci
      
    - name: Build for production
      working-directory: ./Creditor_UserDash
      run: npm run build
      
    - name: Check deployment secrets
      id: check-secrets-prod
      env:
        NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
      run: |
        if [ -n "$NETLIFY_AUTH_TOKEN" ] && [ -n "$NETLIFY_SITE_ID" ]; then
          echo "secrets-available=true" >> $GITHUB_OUTPUT
        else
          echo "secrets-available=false" >> $GITHUB_OUTPUT
        fi
      
    - name: Deploy to Netlify (Production)
      if: steps.check-secrets-prod.outputs.secrets-available == 'true'
      uses: nwtgck/actions-netlify@v2.0
      with:
        publish-dir: './Creditor_UserDash/dist'
        production-deploy: true
        github-token: ${{ secrets.GITHUB_TOKEN }}
        deploy-message: "Deploy from GitHub Actions - Production"
        enable-pull-request-comment: true
        enable-commit-comment: true
        overwrites-pull-request-comment: true
      env:
        NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
